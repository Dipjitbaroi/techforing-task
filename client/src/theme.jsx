import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#182F59", // Primary color (dark blue)
    },
    secondary: {
      main: "#FF5722", // Secondary color (orange)
    },
    black: {
        main: "#000000",
      },
  },
});

export default theme;
